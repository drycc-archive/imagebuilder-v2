apiVersion: v1
kind: ConfigMap
metadata:
  name: dockerbuilder-config
  labels:
    heritage: drycc
data:
  images.json: |
    [
        {
            "name": "container",
            "image": "{{.Values.image_registry}}{{.Values.org}}/dockerbuilder:{{.Values.image_tag}}"
        }
    ]
  image_pull_policy: {{ .Values.image_pull_policy }}
